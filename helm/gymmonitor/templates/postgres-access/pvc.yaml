apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-access-pvc
  labels:
    {{- include "gymmonitor.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: {{ .Values.storage.postgresAccess }}
