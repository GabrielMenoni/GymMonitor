apiVersion: v1
kind: Service
metadata:
  name: access-control
  labels:
    {{- include "gymmonitor.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: access-control
  ports:
    - port: 8081
      targetPort: 8081
