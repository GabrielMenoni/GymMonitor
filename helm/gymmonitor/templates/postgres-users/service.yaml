apiVersion: v1
kind: Service
metadata:
  name: postgres-users
  labels:
    {{- include "gymmonitor.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: postgres-users
  ports:
    - port: 5432
      targetPort: 5432
